<?xml version="1.0" encoding="utf-8"?>
<!-- Just the artwork (gas meter + flame + calculator) on transparency, pre-sized with a comfortable
     margin so it sits nicely inside the icon and is never clipped by launcher masks. The red field
     comes from ic_launcher_background, so there is no seam. -->
<inset xmlns:android="http://schemas.android.com/apk/res/android"
    android:drawable="@drawable/ic_launcher_fg"
    android:inset="0dp" />
